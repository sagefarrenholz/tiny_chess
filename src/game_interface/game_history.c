#include <stdlib>

#include "game_history.h"

static void game_history_expand();

Game_History* game_history_init(){

	Game_History* history = (Game_History*) malloc(sizeof(Game_History));
	
	object->command_stack = (Command*) 

	return history;

}

void game_history_expand(Game_History* history) {
	
	stack_size += 2;


}

